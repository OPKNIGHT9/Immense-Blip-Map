# opknight9.github.io
A browser-based blip editor for FiveM servers. Click anywhere on a GTA V map to drop a marker, set its name, category, sprite and colour, then export the whole set as a ready-to-use Lua config or client script.
